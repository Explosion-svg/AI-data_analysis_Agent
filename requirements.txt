# ── Web Framework ────────────────────────────────────────────────────────────
# P4-1：锁定精确版本，保证可复现。以下版本与本地 venv 完全一致
# （venv 上 58 个测试全部通过的组合）。
# 注意：曾出现锁定版本与 venv 不一致导致 CI 依赖解析失败
# （mcp 1.28.1 要求 pydantic>=2.11，却锁了 2.9.2）--修改此处时
# 务必先 `pip list` 核对真实安装版本。
fastapi==0.135.3
uvicorn[standard]==0.43.0
pydantic==2.12.5
pydantic-settings==2.13.1

# ── LLM / AI ─────────────────────────────────────────────────────────────────
openai==2.30.0
mcp==1.27.0                   # Demo/ MCP server & client

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.49
aiosqlite==0.22.1
asyncpg==0.31.0               # PostgreSQL async driver
aiomysql==0.3.2               # MySQL async driver

# ── Vector Store ─────────────────────────────────────────────────────────────
chromadb==1.5.5

# ── Data Analysis ─────────────────────────────────────────────────────────────
pandas==3.0.2
numpy==2.4.4
tabulate==0.10.0              # df.to_markdown()
plotly==6.6.0                # Interactive charts

# ── Observability ────────────────────────────────────────────────────────────
structlog==25.5.0
prometheus-client==0.24.1

# ── OpenTelemetry (optional) ──────────────────────────────────────────────────
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp-proto-grpc==1.40.0

# ── SQL Safety ────────────────────────────────────────────────────────────────
sqlparse==0.5.5

# ── Reliability ───────────────────────────────────────────────────────────────
# (no extra deps - all implemented in stdlib asyncio)

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest==9.0.2
pytest-asyncio==1.3.0
httpx==0.28.1                 # FastAPI test client
redis==7.4.0                 # Optional: external cache backend

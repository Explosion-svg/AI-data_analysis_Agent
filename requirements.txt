# ── Web Framework ────────────────────────────────────────────────────────────
# P4-1：锁定精确版本，保证可复现（原为 `>=` 浮动范围，同一份 requirements
# 在不同时间安装可能得到不同依赖树）。以下版本为 venv 中实测通过的组合。
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# ── LLM / AI ─────────────────────────────────────────────────────────────────
openai==2.31.0
mcp==1.28.1                  # Demo/ MCP server & client

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.49
aiosqlite==0.22.1
asyncpg==0.31.0               # PostgreSQL async driver
aiomysql==0.3.2               # MySQL async driver

# ── Vector Store ─────────────────────────────────────────────────────────────
chromadb==1.5.7

# ── Data Analysis ─────────────────────────────────────────────────────────────
pandas==3.0.2
numpy==2.4.4
tabulate==0.10.0              # df.to_markdown()
plotly==6.7.0                # Interactive charts

# ── Observability ────────────────────────────────────────────────────────────
structlog==25.5.0
prometheus-client==0.25.0

# ── OpenTelemetry (optional) ──────────────────────────────────────────────────
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0

# ── SQL Safety ────────────────────────────────────────────────────────────────
sqlparse==0.5.5

# ── Reliability ───────────────────────────────────────────────────────────────
# (no extra deps — all implemented in stdlib asyncio)

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.3.0
httpx==0.27.2                 # FastAPI test client
redis==7.4.0                 # Optional: external cache backend
